<div class="trust-strip">
  <div class="page-width trust-strip__inner">
    {%- for block in section.blocks -%}
      <div class="trust-strip__item" {{ block.shopify_attributes }}>
        <span class="coord-dot">◆</span>{{ block.settings.text }}
      </div>
    {%- endfor -%}
  </div>
</div>

{% schema %}
{
  "name": "Trust strip",
  "max_blocks": 4,
  "blocks": [
    {
      "type": "badge",
      "name": "Badge",
      "settings": [{ "type": "text", "id": "text", "label": "Text", "default": "Free remake if we get it wrong" }]
    }
  ],
  "presets": [
    {
      "name": "Trust strip",
      "blocks": [
        { "type": "badge", "settings": { "text": "Engraved to order in the USA" } },
        { "type": "badge", "settings": { "text": "Engraving preview before ordering" } },
        { "type": "badge", "settings": { "text": "Free remake if we get it wrong" } },
        { "type": "badge", "settings": { "text": "Secure checkout" } }
      ]
    }
  ]
}
{% endschema %}
