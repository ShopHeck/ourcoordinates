*,*::before,*::after{box-sizing:border-box}
body{margin:0;font-family:var(--f-body);font-size:16px;line-height:1.6;color:var(--c-ink);background:var(--c-paper);-webkit-font-smoothing:antialiased}
img{max-width:100%;height:auto;display:block}
a{color:inherit}
button{font:inherit;cursor:pointer}
h1,h2,h3{font-family:var(--f-display);font-weight:500;line-height:1.1;margin:0 0 .5em;letter-spacing:-.01em}
h1{font-size:clamp(2.1rem,5.4vw,3.6rem)}
h2{font-size:clamp(1.5rem,3.5vw,2.4rem)}
.page-width{max-width:var(--page-max);margin:0 auto;padding-inline:var(--gutter)}
.section{padding-block:clamp(48px,8vw,96px)}
.skip-link{position:absolute;left:-9999px;top:0;z-index:100;background:var(--c-ink);color:var(--c-paper);padding:10px 16px}
.skip-link:focus{left:0}
:focus-visible{outline:2px solid var(--c-gold);outline-offset:2px}
.coord-eyebrow{font-family:var(--f-coord);font-size:.72rem;letter-spacing:.18em;text-transform:uppercase;color:var(--c-gold-text);display:flex;align-items:center;gap:12px;margin-bottom:14px}
.coord-eyebrow::before{content:"";width:28px;height:1px;background:var(--c-gold)}
.btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;padding:15px 28px;background:var(--c-ink);color:var(--c-paper);border:1px solid var(--c-ink);border-radius:2px;font-weight:600;font-size:.92rem;letter-spacing:.04em;text-transform:uppercase;text-decoration:none;transition:background .2s,color .2s}
.btn:hover{background:var(--c-gold);border-color:var(--c-gold);color:var(--c-ink)}
.announcement{background:var(--c-ink);color:var(--c-paper);text-align:center;padding:9px var(--gutter);font-family:var(--f-coord);font-size:.74rem;letter-spacing:.14em;text-transform:uppercase}
.announcement strong{color:var(--c-gold);font-weight:500}
.site-header{position:sticky;top:0;z-index:50;background:var(--c-paper);border-bottom:1px solid var(--c-line)}
.site-header__inner{display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:16px;padding-block:14px}
.site-header__logo{justify-self:center;text-decoration:none;font-family:var(--f-display);font-size:1.35rem}
.site-header__logo img{max-height:34px;width:auto}
.site-nav{display:flex;gap:22px;align-items:center}
.site-nav>a,.site-nav__mega-trigger{text-decoration:none;font-size:.86rem;font-weight:600;letter-spacing:.05em;text-transform:uppercase;padding-block:4px;border-bottom:1px solid transparent}
.site-nav__mega-trigger{display:flex;align-items:center;gap:7px;cursor:pointer;list-style:none}
.site-nav__mega-trigger::-webkit-details-marker{display:none}
.site-nav__mega-trigger svg{width:11px;height:7px;fill:none;stroke:currentColor;stroke-width:1.5}
.mega-menu{display:none;position:absolute;left:0;right:0;top:100%;background:var(--c-paper)}
.site-nav__mega[open]>.mega-menu{display:block}
.site-header__actions{justify-self:end;display:flex;gap:16px;align-items:center}
.site-header__actions a{text-decoration:none;font-size:.86rem;font-weight:600}
.cart-count{display:inline-grid;place-items:center;min-width:19px;height:19px;padding-inline:4px;background:var(--c-gold);color:var(--c-ink);border-radius:999px;font-family:var(--f-coord);font-size:.68rem}
.menu-toggle{display:none;background:none;border:0;padding:6px}
.menu-toggle span{display:block;width:22px;height:2px;background:var(--c-ink);margin:5px 0}
@media (max-width:860px){
.site-header__inner{grid-template-columns:auto 1fr auto}
.site-header__logo{justify-self:start}
.menu-toggle{display:block}
.site-nav{display:none;position:absolute;left:0;right:0;top:100%;background:var(--c-paper);border-bottom:1px solid var(--c-line);flex-direction:column;gap:0;padding:8px var(--gutter) 16px}
.site-nav.is-open{display:flex}
.site-nav>a,.site-nav__mega-trigger{width:100%;padding-block:12px;border-bottom:1px solid var(--c-line)}
.site-nav__mega-trigger{justify-content:space-between}
.mega-menu{position:static}
}
.hero{position:relative;color:#fff;background:var(--c-graphite);overflow:hidden;min-height:clamp(440px,68vh,660px);display:grid;align-items:center}
.hero__media{position:absolute;inset:0}
.hero__media img{width:100%;height:100%;object-fit:cover;opacity:.5}
.hero__content{position:relative;max-width:640px;padding-block:64px}
.hero h1{color:#fff}
.hero p{font-size:1.05rem;color:rgba(255,255,255,.78);max-width:46ch}
.hero .btn{background:var(--c-gold);border-color:var(--c-gold);color:var(--c-ink)}
.pdp{display:grid;grid-template-columns:minmax(0,1.1fr) minmax(0,1fr);gap:clamp(28px,5vw,64px);align-items:start}
.pdp__gallery{position:sticky;top:84px;display:grid;gap:10px;min-width:0}
.pdp__main-image{aspect-ratio:1;background:#fff;border:1px solid var(--c-line);overflow:hidden}
.pdp__main-image img{width:100%;height:100%;object-fit:cover}
.pdp__thumbs{display:grid;grid-template-columns:repeat(6,1fr);gap:8px}
.pdp__thumbs button{padding:0;border:1px solid var(--c-line);background:#fff;aspect-ratio:1;overflow:hidden}
.pdp__thumbs img{width:100%;height:100%;object-fit:cover}
@media(max-width:880px){.pdp{grid-template-columns:1fr}.pdp__gallery{position:static}}
.meridian-ruler{display:none}
@media (min-width:1100px){
.meridian-ruler{position:fixed;right:10px;top:0;bottom:0;z-index:40;display:flex;flex-direction:column;justify-content:space-between;padding-block:120px;font-family:var(--f-coord);font-size:.58rem;color:var(--c-ink-soft);pointer-events:none}
.meridian-ruler span{display:flex;align-items:center;gap:6px}
.meridian-ruler span::after{content:"";width:10px;height:1px;background:var(--c-ink-soft)}
}
.engrave-help{margin:6px 0 0;font-size:.78rem;line-height:1.35;color:var(--c-ink-soft)}
.set-upsell{display:flex;align-items:center;gap:12px;margin:12px 0 14px;padding:10px 12px;border:1px solid var(--c-line);border-radius:2px;text-decoration:none;color:inherit;background:#fff;transition:border-color .15s ease}
.set-upsell:hover{border-color:var(--c-gold)}
.set-upsell img{width:56px;height:56px;object-fit:cover;border-radius:2px;flex-shrink:0}
.set-upsell__text{flex:1;min-width:0;display:flex;flex-direction:column;gap:2px}
.set-upsell__text strong{font-size:.9rem;font-weight:600}
.set-upsell__text em{font-style:normal;font-size:.78rem;color:var(--c-ink-soft);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.set-upsell__cta{font-size:.78rem;font-weight:600;color:var(--c-gold-text);white-space:nowrap}

/* === ENGRAVING PREVIEWS (all piece types) === */
.ep{background:linear-gradient(120deg,#d8d3c9,#efece5 45%,#cfc9bd);border:1px solid var(--c-line);border-radius:3px;padding:24px 16px 12px;text-align:center;margin-bottom:10px}
.ep__hint{font-family:var(--f-coord);font-size:.66rem;letter-spacing:.14em;text-transform:uppercase;color:var(--c-ink-soft);margin-top:10px;margin-bottom:0}
.ep svg{max-width:100%;height:auto;display:block;margin:0 auto;overflow:visible}
.ep__engrave-text{fill:#cdb88a;font-family:var(--f-coord);font-size:12px;letter-spacing:.18em;text-anchor:middle;dominant-baseline:middle}
.ep__engrave-text--curve{letter-spacing:.12em}
.ep__engrave-text--vertical{writing-mode:tb;glyph-orientation-vertical:0;text-anchor:middle;letter-spacing:.28em;font-size:11px}
.ep__metal{fill:#1c1c24}
.ep svg .ep__metal{fill:url(#ep-metal-black);filter:drop-shadow(0 4px 10px rgba(0,0,0,.25))}
[data-metal=gold] .ep svg .ep__metal,[data-metal="Gold"] .ep svg .ep__metal{fill:url(#ep-metal-gold)}
[data-metal="rose-gold"] .ep svg .ep__metal,[data-metal="Rose Gold"] .ep svg .ep__metal{fill:url(#ep-metal-rose-gold)}
[data-metal=silver] .ep svg .ep__metal,[data-metal=Silver] .ep svg .ep__metal{fill:url(#ep-metal-silver)}
[data-metal=black] .ep svg .ep__metal,[data-metal=Black] .ep svg .ep__metal{fill:url(#ep-metal-black)}
.ep__ring-band{stroke:url(#ep-metal-black);fill:none}
[data-metal=gold] .ep__ring-band{stroke:url(#ep-metal-gold)}
[data-metal="rose-gold"] .ep__ring-band{stroke:url(#ep-metal-rose-gold)}
[data-metal=silver] .ep__ring-band{stroke:url(#ep-metal-silver)}
[data-metal=black] .ep__ring-band{stroke:url(#ep-metal-black)}
.ep__cuff-sheen{stroke:rgba(255,255,255,.13);fill:none;stroke-linecap:round}
.ep__cuff-rim{stroke:rgba(255,255,255,.22);fill:none}
.ep__cuff-inner-shadow{stroke:rgba(0,0,0,.3);fill:none}
.ep__cuff-channel{stroke:rgba(10,12,16,.42);fill:none;stroke-linecap:round}
.ep__cuff-channel--core{stroke:rgba(10,12,16,.25)}
.ep__cuff-engrave{fill:#f2e9d6}
[data-metal=gold] .ep .ep__cuff-engrave,[data-metal="rose-gold"] .ep .ep__cuff-engrave,[data-metal=silver] .ep .ep__cuff-engrave{fill:#f7f1e2}
.ep__sheen{fill:rgba(255,255,255,.12)}
.ep__bevel{opacity:.35}
.ep__hole{fill:var(--c-paper)}
.ep__set-svg{width:100%;height:auto;overflow:visible}

/* Bracelet */
.ep--bracelet{padding:24px 8px 12px}
.ep--bracelet svg,.ep__set-svg--bracelet{width:min(100%,300px)}

/* Horizontal bar */
.ep--horizontal-bar{padding:28px 16px 12px}
.ep__hbar-svg{max-width:320px;width:100%;height:auto;display:block;margin:0 auto}

/* Vertical bar */
.ep--vertical-bar{padding:20px 16px 12px}
.ep__vbar-svg{max-width:80px;width:100%;height:auto;display:block;margin:0 auto}

/* Ring */
.ep--ring{padding:24px 12px 12px}
.ep__ring-svg{max-width:260px;width:100%;height:auto;display:block;margin:0 auto}

/* Four-sided necklace */
.ep--four-sided{padding:12px}
.ep__chain{margin-bottom:-5px}
.ep__chain svg{width:min(300px,92%);height:auto;display:block;margin:0 auto}
.ep__chain-line{stroke:#8b8b95;stroke-width:1.3;fill:none}
[data-metal=gold] .ep__chain-line,[data-metal="Gold"] .ep__chain-line{stroke:#b7975f}
[data-metal="rose-gold"] .ep__chain-line,[data-metal="Rose Gold"] .ep__chain-line{stroke:#c48f76}
[data-metal=silver] .ep__chain-line,[data-metal=Silver] .ep__chain-line{stroke:#b0b0ba}
.ep__faces{display:flex;justify-content:center;align-items:flex-end;gap:18px;margin-bottom:4px}
.ep__face{text-align:center;transition:opacity .25s ease,filter .25s ease}
.ep__face[data-face-active="false"]{opacity:.2;filter:grayscale(1)}
.ep__face-svg{width:46px;height:auto;display:block;margin:0 auto}
.ep__face-label{font-family:var(--f-coord);font-size:.6rem;letter-spacing:.14em;text-transform:uppercase;color:var(--c-ink-soft);margin-top:4px;display:block}
.ep__side-picker{margin-bottom:12px}
.ep__side-picker legend{margin-bottom:6px}
.ep__side-input{margin-bottom:8px}
.ep__side-input label{font-size:.78rem;font-weight:600;letter-spacing:.04em;text-transform:uppercase;color:var(--c-ink-soft);display:block;margin-bottom:4px}
.ep__side-optional{text-transform:none;letter-spacing:0;font-weight:400;font-size:.72rem;color:var(--c-ink-soft)}
.ep__side-tip{font-size:.78rem;color:var(--c-ink-soft);background:rgba(182,151,106,.08);border-left:2px solid var(--c-gold);padding:8px 10px;margin:0 0 10px;border-radius:2px}

/* Dog tag */
.ep--dog-tag{padding:16px 12px}
.ep__dog-tag-svg{width:min(100%,180px)}

/* Star map disc */
.ep--star-map{padding:14px 12px 12px}
.ep__sm-svg{max-width:252px;width:100%;height:auto;display:block;margin:0 auto}
.ep__sm-ring{stroke:rgba(234,226,200,.22);stroke-width:.8}
.ep__sm-star{fill:#efe9d8}
.ep__sm-star--bright{filter:drop-shadow(0 0 1.4px rgba(239,233,216,.9))}
.ep__sm-line{stroke:rgba(234,226,200,.3);stroke-width:.55}
.ep__sm-label{fill:rgba(234,226,200,.58);font-family:var(--f-coord);font-size:5.5px;letter-spacing:.08em;text-anchor:middle}
.ep__sm-cardinal{fill:rgba(234,226,200,.42);font-family:var(--f-coord);font-size:7px;letter-spacing:.1em;text-anchor:middle}
.ep__sm-rule{stroke:rgba(234,226,200,.35);stroke-width:.6}
.ep__sm-text{fill:#ead9b0;font-family:var(--f-coord);font-size:11px;letter-spacing:.2em;text-anchor:middle}
.ep__sm-text--small{font-size:7.5px;letter-spacing:.24em;fill:rgba(234,217,176,.85)}
.ep__sm-fields{display:grid;grid-template-columns:1fr 1fr;gap:0 10px}
.ep__sm-field{margin-bottom:8px}
.ep__sm-field label{font-size:.78rem;font-weight:600;letter-spacing:.04em;text-transform:uppercase;color:var(--c-ink-soft);display:block;margin-bottom:4px}
.ep__sm-field input[type=date],.ep__sm-field input[type=time]{width:100%;padding:11px 12px;border:1px solid var(--c-line);border-radius:2px;background:#fff;font-family:var(--f-coord)}
.ep__sm-field input[type=date]:focus,.ep__sm-field input[type=time]:focus{border-color:var(--c-gold);outline:none}
.ep--star-map .ep__hint[data-state=ready]{color:#35634a}
.ep--star-map .ep__hint[data-state=error]{color:#8b3e35}
@media(max-width:520px){.ep__sm-fields{grid-template-columns:1fr}}

/* Set / duo */
.ep--set{padding:16px 12px}
.ep--duo{display:grid;gap:12px}

@media (max-width:420px){
.ep__faces{gap:10px}
.ep__face-svg{width:38px}
.ep__ring-svg{max-width:200px}
.ep--bracelet .ep__set-svg--bracelet{max-width:240px}
}

/* Collection filters */
.collection-controls{display:flex;flex-wrap:wrap;align-items:flex-start;gap:10px;margin:0 0 28px;padding-block:14px;border-block:1px solid var(--c-line)}
.collection-controls__sort{display:flex;align-items:center;gap:8px}
.collection-controls select,.collection-filter summary{min-height:42px;padding:9px 12px;border:1px solid var(--c-line);background:#fff;font:inherit}
.collection-filter{position:relative}
.collection-filter summary{cursor:pointer;list-style:none}
.collection-filter summary::-webkit-details-marker{display:none}
.collection-filter__body{position:absolute;z-index:20;top:calc(100% + 4px);left:0;min-width:240px;max-height:320px;overflow:auto;padding:14px;border:1px solid var(--c-line);background:#fff;box-shadow:0 14px 34px rgba(11,11,13,.12)}
.collection-filter__body>label{display:flex;gap:8px;padding:6px 0}
.collection-filter__price{display:flex;gap:8px}
.collection-filter__price input{display:block;width:100px;margin-top:4px;padding:8px;border:1px solid var(--c-line)}
.collection-controls__apply{min-height:42px;padding-block:9px}
